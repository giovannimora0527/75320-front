export interface PrestamoRq {
    usuarioId: number;          // ID del usuario seleccionado
    libroId: number;            // ID del libro seleccionado
    fechaDevolucion: string;    // Fecha en formato 'yyyy-MM-dd'
}
